# action-on-staging (deprecated)

This repository is **deprecated**. The composite action has been migrated to [emo-workflows](https://github.com/emocional/emo-workflows).

Use instead:

```yaml
uses: emocional/emo-workflows/.github/actions/on-staging@main
```

Source: `.github/actions/on-staging/action.yml` in `emo-workflows`.
